для обнуления базы данных проекта:
docker compose down -v
docker compose up -d --build
docker compose exec web python manage.py migrate
docker compose exec web python manage.py createsuperuser #нужно выполнить один раз создает профиль админа



для git:
git add .
git commit -m "sdfgjdfbvhdscuzvjd короче тут комментарии зачем я ввел изменение" 
git push origin master

Протокол общение с GPT:
необходим чтобы не гонять одни и те же ошибки по кругу
необходимо использовать единый namespaced-стиль (urls.py всегда app_name = "...", все переходы <a href=""> через {% url %})
необходимо при регистрации контролировать пароль (длина не менее 8 символов, не использовать только цифры и т.д) #постоянно слетает, на данный момент опять слетело

Вот что я бы пока не тащил:
часы волонтёрства
сложный ML endpoint
сертификаты как обязательную сущность
Telegram/email-автоматику
модератора
Это можно оставить как расширение.

🧠 Логика СППР (ядро)
1. Жёсткая фильтрация

город

доступность

базовые требования

2. Scoring
score =
0.4 навыки +
0.3 опыт +
0.2 расстояние +
0.1 рейтинг
3. Сортировка → топ N

